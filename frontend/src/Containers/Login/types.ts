export enum Tab {
  SIGN_IN = 'Sign in',
  SIGN_UP = 'Sign up',
}

export type FormikProps = {
  email: string;
  password: string;
  regEmail: string;
  regPassword: string;
  bussinessName: string;
  confirmPassword: string;
};
